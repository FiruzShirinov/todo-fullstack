# Base URL of the Laravel API (no trailing slash).
# Local dev default:
NUXT_PUBLIC_API_BASE=http://localhost:8000/api

# Host/port the Nuxt server binds to (used by `nuxt preview` / node server).
# HOST=0.0.0.0
# PORT=3000
